# Stream Vibe  
*Проект выполнен в рамках обучения Frontend-разработке.*

Проект стримингового сервиса, реализованный на базе **Minista**.
Сайт представляет собой многостраничное приложение с адаптивной версткой, 
сложными компонентами и интерактивными элементами.


## 🛠 Стек технологий:
- **Framework:** [Minista](https://minista.js.org/) (Static Site Generator на базе React/JSX)
- **Styling:** SCSS (препроцессор), БЭМ-методология, fluid typography (динамические размеры)
- **Language:** JavaScript (ES6+), JSX
- **Build Tools:** Vite (сборка), PostCSS (автоматизация CSS: PxToRem, PresetEnv)
- **Core Features:**
    - Динамическая адаптивность (Fluid Typography & Spacing)
    - Слайдеры на базе [Swiper.js](https://swiperjs.com/)
    - Интерактивные компоненты: табы, аккордеоны, модальные окна, видеоплеер
    - Работа с формами и масками ввода ([IMask](https://imask.js.org/))


## 🚀 Функционал:
- **Главная страница:** Hero-секция, список категорий и планов подписки.
- **Страница фильмов:** Полноценный плеер, система рейтинга, фильтрация контента.
- **Интерактивность:** Реализована через JS-модули (OverlayMenu, SliderCollection, TabsCollection).
- **SEO & Performance:** Оптимизация изображений, шрифты, использование современной методологии БЭМ.


## 📝 Changelog (История разработки)

<details>
<summary>

### 1.5: Файлы constants.scss и functions.scss — Sass-константы и функции
</summary>

Реализована система плавного масштабирования контента с помощью функций **Sass** и CSS-свойства `clamp()`.
* **Функции `fluid()` и `fluid-to-laptop()`**: на основе математической модели и CSS-свойства `clamp()` автоматически вычисляют динамические размеры шрифтов и отступов.
* **Преимущества**:
    * **Плавность**: контент масштабируется непрерывно, без резких переходов.
    * **Лаконичность**: существенное сокращение объема CSS-кода.
    * **Гибкость**: централизованное управление через глобальные переменные в `constants.scss`.
</details>

<details>
<summary>

### 1.6: Файл mixins.scss — Sass-миксины 🛠 UI Toolkit
</summary>

Разработана библиотека миксинов для ускорения разработки и поддержания чистоты стилей:
* **Layout:** `flex-center`, `abs-cover`, `abs-center` и другие — упрощают позиционирование элементов.
* **Typography:** `fluid-text` — автоматизирует применение функции `fluid()` к размерам шрифтов.
* **Utilities:** `visually-hidden` (для доступности), `focus-visible` (единый стиль фокуса), `disabled` (стандартное состояние отключенных элементов).
</details>

<details>
<summary>

### 1.7: Файл media.scss — Sass-миксины для медиа-запросов
</summary>

Реализован набор миксинов для удобного управления медиа-запросами и состояниями элементов.
* **Адаптивные миксины:** Группа миксинов (`@include mobile`, `@include tablet` и др.) упрощает написание адаптивного кода, скрывая сложность медиа-запросов за интуитивными названиями.
* **Smart Hover:** Миксин `@include hover` корректно обрабатывает состояния наведения, учитывая особенности сенсорных устройств (отключает «залипание» hover-эффектов на мобильных).
</details>

<details>
<summary>

### 1.8: Модульная система стилей и агрегация через index.scss
</summary>

Реализована модульная система стилей с использованием `@forward`.
* **Единый импорт:** Использование файла-агрегатора, который собирает все константы, функции, миксины и медиа-запросы в одну точку входа.
* **Масштабируемость:** Добавление новых инструментов требует внесения правок только в один файл `helpers/index.scss`.
</details>

<details>
<summary>

### 1.9: Сброс и базовая настройка стилей
</summary>

Реализован комплексный файл нормализации для обеспечения единообразного отображения во всех браузерах.
* **Унификация:** Настройка `border-box`, сброс стандартных отступов, удаление маркеров списков.
* **Оптимизация:** Плавный скролл, устранение скачков скроллбара, авто-окраска SVG через `currentColor`.
* **Доступность:** Поддержка `prefers-reduced-motion` для управления анимациями.
</details>

<details>
<summary>

### 1.10: Подключение шрифтовой гарнитуры Manrope
</summary>

Интегрирован основной шрифт проекта с настройкой начертаний и оптимизацией загрузки.
* **FontFace:** Подключены современные форматы `woff2` (400–700).
* **Оптимизация:** Использование `font-display: swap` для предотвращения эффекта «скачка» текста.
* **JS Entry Point:** Шрифты импортированы через `index.js`, обеспечивая автоматическую обработку путей сборщиком (Vite).
</details>

<details>
<summary>

### 1.11: Глобальные CSS-переменные и дизайн-токены
</summary>

Реализована система централизованного управления стилями через CSS Variables (`:root`).
* **Централизация:** Все ключевые значения вынесены в `:root` для доступности по всей DOM-структуре.
* **Динамика vs Статика:** Использование нативных CSS-переменных вместо Sass-переменных позволяет изменять параметры через JavaScript «на лету».
* **Семантическая палитра:** Использование цветовых шкал (red, black, gray) с цифровыми индексами.
</details>

<details>
<summary>

### 1.12: Утилитарные классы и вспомогательные стили
</summary>

Добавлен набор глобальных утилитарных классов для ускорения верстки.
* **Центрирующий контейнер:** `.container` с гибкой логикой ограничения ширины.
* **Доступность:** `.visually-hidden` для сохранения семантики при визуальном скрытии элементов.
* **Адаптивные хелперы:** Система классов (например, `.hidden-mobile`) для управления видимостью блоков на основе медиа-миксинов.
</details>

<details>
<summary>

### 1.13: Базовые глобальные стили и типографика
</summary>

Завершена настройка основных стилей документа и иерархии заголовков.
* **Резиновая типографика:** Интеграция `@include fluid-text` для заголовков и текста.
* **Структура страницы:** Настройка flex-модели для `body`/`main` и логика блокировки прокрутки (`is-lock`).
* **Интерактивные состояния:** Глобальные `transition`, стили фокуса и hover-эффектов.
</details>

<details>
<summary>

### 1.15 : Глобальная структура Layout: Header, Content, Footer и метаданные
</summary>

Реализован основной каркас приложения на базе Minista, объединяющий базовые компоненты страницы и настройки секции Head.
* **Архитектура Layout:** Создана общая структура страницы с использованием компонентов `Header`, `Content` и `Footer`, что обеспечивает единообразие всех страниц проекта.
* **Управление метаданными:** В секции `<Head>` настроены динамические заголовки, атрибуты языка и подключение фавиконок (apple-touch-icon, manifest, favicon-32x32/16x16).
* **Точка входа скриптов:** Добавлена интеграция файла `main.js`, который служит центральным узлом для импорта всех JavaScript-модулей и логики проекта.
* **Глобальные стили:** Импорт `@/styles` в основном шаблоне гарантирует применение базовых стилей и переменных ко всему приложению.
</details>